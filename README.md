# ParasiteEngine

## Project Setup:

Clone the repository via ```git clone --recursive https://github.com/ajbarnes97/ParasiteEngine```

If the repository was cloned non-recursively the submodules can be retrieved via ```git submodule update --init```
